class Queue {
    constructor(){
        this.items = [];
    }
}

const enqueue = async (element) => {
    // adding element to the queue
    this.items.push(element);
}

const dequeue = async () => {

}