let sumOfTwoRevLists = async () => {

    let result = [];
    let arr1 = [1,2,3];
    let arr2 = [4,5,6];

    let len = arr1.length > arr2.length ? arr1.length : arr2.length;
    // arr1 = arr1.reverse;
    // arr2 = arr2.reverse;
    
    for(let i = 0; i < len; i++){
        if(arr1[i] != undefined && arr2[i] != undefined){

        }
        else if(arr1 != undefined){

        }
        else{

        }
    }
}

sumOfTwoRevLists();